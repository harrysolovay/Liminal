import { outdent } from "@cspotcode/outdent"

export const DEFAULT_INSTRUCTIONS: string = outdent`
  Analyze user messages and synthesize structured outputs.
  Ensure your responses are logically consistent, clear, and follow the requested format.
`
